# pxe-beacon EMBEDDED DEFAULT preseed.cfg
#
# Used when a debian-12 / debian-13 fleet entry omits the `preseed:`
# field. Provides a minimum-viable unattended install so operators
# can verify the boot chain without writing a preseed first.
#
# *** SECURITY: the default user is `pxe` / password `pxe`. This is
# *** INTENTIONALLY weak — it exists so the boot works out of the
# *** box. For anything other than a throwaway test machine, write
# *** your own preseed and reference it via `preseed: ./your.cfg`
# *** in fleet.yaml, or edit this template via the /admin UI.
#
# pxe-beacon Go-templates this file. Vars: Name, MAC, MACHyp,
# AdvertisedIP, HTTPPort, WiderNetworkCIDR.
#
# v0.6.8: rewritten to mirror a known-good real-world preseed
# (venkatamutyala/debian-preseed) after our v0.5/v0.6 hardening
# turned out to cause Debian to crash on UEFI bare metal. Key
# changes from v0.6.7:
#   - REMOVED `partman-efi/non_efi_system boolean true` — that
#     option forces BIOS-compat mode (we'd misread it as "UEFI
#     hardening"); on real UEFI hardware it breaks partitioning.
#   - REMOVED `grub-installer/force-efi-extra-removable` — pointless
#     for fresh installs.
#   - SIMPLIFIED disk selection — `lsblk` may not be in d-i's
#     busybox; use plain `[ -b ... ]` block-device tests.
#   - ADDED `seen true` on hostname/domain — without it d-i still
#     prompts even with values set.
#   - SIMPLIFIED tasksel — just `standard`, not `standard, ssh-server`
#   - SIMPLIFIED late_command — line-by-line echos instead of a
#     printf heredoc.

### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

### Network — `seen true` suppresses the prompt even though we
### provided values.
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string {{.Name}}
d-i netcfg/get_hostname seen true
d-i netcfg/get_domain string local
d-i netcfg/get_domain seen true

### Mirror — HTTP not HTTPS (snponly iPXE's CA + clock are unreliable;
### d-i loads its own apt config later)
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Account setup — DEFAULT IS INSECURE (pxe / pxe). Replace by
### providing a custom preseed via fleet.yaml's `preseed:` field or
### overriding via the /admin UI.
d-i passwd/root-login boolean false
d-i passwd/make-user boolean true
d-i passwd/user-fullname string pxe-beacon default
d-i passwd/username string pxe
d-i passwd/user-password password pxe
d-i passwd/user-password-again password pxe
d-i user-setup/allow-password-weak boolean true
d-i user-setup/encrypt-home boolean false

### Clock
d-i clock-setup/utc boolean true
d-i time/zone string Etc/UTC
d-i clock-setup/ntp boolean true

### Disk selection — try NVMe first, then SATA, then virtio, then IDE.
### Use POSIX `[ -b path ]` block-device test (works in d-i's busybox;
### `lsblk` is NOT always present). Operators with multi-disk setups
### should provide their own preseed.
d-i partman/early_command string \
  for d in /dev/nvme0n1 /dev/sda /dev/vda /dev/hda; do \
    if [ -b "$d" ]; then \
      debconf-set partman-auto/disk "$d"; \
      debconf-set grub-installer/bootdev "$d"; \
      break; \
    fi; \
  done

d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Base + packages
tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server cloud-init ca-certificates curl sudo
d-i pkgsel/upgrade select none
popularity-contest popularity-contest/participate boolean false

### Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true

### Finish
d-i finish-install/reboot_in_progress note

{{- if .WiderNetworkCIDR }}
### v0.6.0: link-scope route helper for cross-/24-with-/16-bridge
### topologies (Mac/server on Wi-Fi subnet, this box on wired LAN,
### same L2 broadcast domain, DHCP server hands out /24). Without
### this route, cloud-init phone_home back to {{.AdvertisedIP}}
### would route via the gateway and fail.
###
### v0.6.8: simplified to line-by-line echos — the v0.6.0 printf
### heredoc had nested-quote escaping that was fragile.
d-i preseed/late_command string \
  echo "#!/bin/sh" > /target/usr/local/sbin/pxe-beacon-route; \
  echo "set -e" >> /target/usr/local/sbin/pxe-beacon-route; \
  echo "IFACE=\$(ip -o -4 route show default | awk '{print \$5; exit}')" >> /target/usr/local/sbin/pxe-beacon-route; \
  echo "[ -n \"\$IFACE\" ] || exit 0" >> /target/usr/local/sbin/pxe-beacon-route; \
  echo "ip route replace {{.WiderNetworkCIDR}} dev \"\$IFACE\" scope link 2>/dev/null || true" >> /target/usr/local/sbin/pxe-beacon-route; \
  chmod 755 /target/usr/local/sbin/pxe-beacon-route; \
  echo "[Unit]" > /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "Description=pxe-beacon link-scope route to {{.WiderNetworkCIDR}}" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "After=network-online.target" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "Wants=network-online.target" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "[Service]" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "Type=oneshot" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "ExecStart=/usr/local/sbin/pxe-beacon-route" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "RemainAfterExit=yes" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "[Install]" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  echo "WantedBy=multi-user.target" >> /target/etc/systemd/system/pxe-beacon-route.service; \
  in-target systemctl enable pxe-beacon-route.service
{{- end }}
